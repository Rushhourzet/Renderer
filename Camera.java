public class Camera{
    /* 
    g = globalTransform (before rotation)
    l = localTransform (after rot)
    r = rotation in Radians

    s = sensorsize
    o = offset

    f = focalPoint
    sk = skew
    */
    Point3D<Double> g, l, r; 
    Point2D<Double> s, o;
    private double f, sk;


    //Transform before
    
}